version: "3"

tasks:
  install:
    # TODO: formalize the implied dependency on python / pip
    desc: Install poetry
    status:
      - pip show poetry &>/dev/null
    cmds:
      - pip install poetry
