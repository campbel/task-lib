version: "3"

includes:
  os: ./task_{{OS}}.yaml

tasks:
  pyenv:
    desc: Install pyenv
    cmds:
      - task: os:pyenv

  install:
    desc: Install python version
    deps: [pyenv]
    vars:
      VERSION: "3.11"
    cmds:
      - pyenv install {{.VERSION}}
