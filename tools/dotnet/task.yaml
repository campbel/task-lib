version: "3"

includes:
  os: ./task_{{OS}}.yaml

tasks:
  install_latest:
    desc: Install dotnet
    cmds:
      - task: os:install_latest
  install_6:
    desc: Install dotnet
    cmds:
      - task: os:install_6
